基础题单选7*3+不定项4*3
c++题单选2*3+不定项2*3
算法55分：签到送分，全排列+gcd，第三道看都没看

知识点：
1. c++继承时构造函数调用顺序：虚基类，基类，派生类成员类，派生类
2. c++类初始化方式不同，会调用构造函数，或者拷贝构造函数
3. 行为型模式有哪些：观察者模式，策略模式，命令模式，状态模式，职责链模式
（创建型模式有单例模式，工厂模式）（结构型模式有代理模式，桥接模式）
4. 退款适合命令模式（退款操作包装成一个命令，命令需要执行一连串的操作，如修改订单状态、更新账户余额、、更新库存等），观察者模式侧重描述一对多的依赖关系。
5. c++元组tuple：不能[]索引，可以存不同类型元素，支持比较（两个元组的元素按顺序逐个比较）
6. 最佳置换：内存中在未来时间最不会被访问的页面将被置换出
7. KMP算法：匹配表存子串的最长公共前后缀长度，匹配表再整体右移然后第一位补-1得到next数组；两个指针分别指向主串和模式串，比较相等则指针都向后移动，不相等则根据next数组调整模式串指针，继续比较，匹配到末尾结束则匹配成功，否则匹配未成功。
8. Linux命令使用：find /var/log -name "*.txt"
9. 排序算法复杂度
10. MySQL命令
11. HTTP Authorization：认证客户端，保护资源
12. 
